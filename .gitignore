# Python
.venv/
__pycache__/
*.pyc
.uv/

# dbt / DuckDB
transform/target/
transform/dbt_packages/
transform/logs/
transform/.user.yml
*.duckdb
*.duckdb.wal

# Node / Astro
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.astro/
frontend/.vercel/

# Tiles (built artifacts, not committed)
*.pmtiles
*.mbtiles
# …except the committed foundation sample tile, so the Vercel preview renders
# from git. The nightly workflow (v1.0.5) rebuilds it from the full feeds.
!frontend/public/transit.pmtiles

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
